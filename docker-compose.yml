services:
  ai-packaging-demo:
    build: .
    container_name: ai-packaging-demo
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
